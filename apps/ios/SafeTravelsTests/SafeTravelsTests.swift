//
//  SafeTravelsTests.swift
//  SafeTravelsTests
//
//  Created by Mathijs Bernson on 09/07/2025.
//

import Foundation
import Testing
import SwiftData
@testable import SafeTravels

class PolarstepsImporterTests {

//    @Test func importTestData() throws {
//        let schema = Schema([Trip.self, Post.self, Location.self])
//        let modelConfiguration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)
//        let container = try ModelContainer(for: schema, configurations: [modelConfiguration])
//        let context = ModelContext(container)
//        let importer = PolarstepsImporter()
//
//        let bundle = Bundle(for: PolarstepsImporterTests.self)
//        let locationsURL = bundle.url(forResource: "locations", withExtension: "json")!
//        let tripURL = bundle.url(forResource: "trip", withExtension: "json")!
//        try importer.import(locationsFromURL: locationsURL, tripFromURL: tripURL, into: context)
//        try context.save()
//    }

}
